services:
  minio:
    image: quay.io/minio/aistor/minio:latest
    container_name: minio
    restart: always
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - ./storage:/data
      - ./minio.license:/minio.license
    environment:
      MINIO_ROOT_USER: root
      MINIO_ROOT_PASSWORD: Anhvan@1310
      MINIO_BROWSER_REDIRECT_URL: "http://minio.istech.vn"
      MINIO_SERVER_URL: "http://s3.istech.vn"
    command: server --console-address ":9001" /data --license /minio.license